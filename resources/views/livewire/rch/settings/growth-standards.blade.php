<x-card><h2 class="font-semibold">Growth Standards Import Foundation</h2><p class="mt-2 text-sm text-slate-500">Rows imported: {{ number_format($count) }}</p><p class="mt-1 text-sm text-slate-500">Use <code>php artisan growth-standards:import file.csv</code> with verified source data.</p><div class="mt-4 flex flex-wrap gap-2">@foreach($standards as $standard)<x-badge>{{ $standard }}</x-badge>@endforeach</div></x-card>
