<x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><tbody>@forelse($payments as $payment)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="px-3 py-3"><a class="font-semibold text-primary" href="{{ route('insurance.payments.show',$payment) }}">{{ $payment->payment_reference }}</a></td><td class="px-3 py-3">{{ $payment->payment_date?->format('Y-m-d') }}</td><td class="px-3 py-3">{{ number_format($payment->amount,2) }}</td><td class="px-3 py-3">{{ $payment->status }}</td></tr>@empty<tr><td class="px-3 py-8 text-center text-slate-500">Hakuna payments.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $payments->links() }}</div></x-card>
