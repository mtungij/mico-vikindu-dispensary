<div class="space-y-6">
    <div class="grid gap-4 md:grid-cols-3 xl:grid-cols-6">@foreach($cards as $label=>$value)<x-card><p class="text-xs font-medium uppercase text-slate-500">{{ $label }}</p><p class="mt-2 text-2xl font-semibold">{{ number_format($value) }}</p></x-card>@endforeach</div>
    <x-card><div class="mb-4 flex items-center justify-between"><h3 class="font-semibold">Procedures za Karibuni</h3><a href="{{ route('dental.index') }}" class="text-sm text-primary">Foleni</a></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-slate-500"><th class="p-2">Procedure</th><th>Patient</th><th>Dentist</th><th>Status</th></tr></thead><tbody>@forelse($recentProcedures as $p)<tr class="border-t border-slate-200 dark:border-slate-700"><td class="p-2">{{ $p->procedure_name_snapshot }}</td><td>{{ $p->patient?->fullName() }}</td><td>{{ $p->performer?->name }}</td><td><x-badge tone="info">{{ $p->status?->value ?? $p->status }}</x-badge></td></tr>@empty<tr><td colspan="4" class="p-6"><x-empty-state icon="badge-plus" title="Hakuna procedures" /></td></tr>@endforelse</tbody></table></div></x-card>
</div>
