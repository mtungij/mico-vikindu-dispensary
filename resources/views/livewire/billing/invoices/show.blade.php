<div class="space-y-4">
    <div class="grid gap-4 lg:grid-cols-4"><x-card><p class="text-sm text-slate-500">Patient</p><p class="font-semibold">{{ $invoice->patient?->first_name }} {{ $invoice->patient?->last_name }}</p></x-card><x-card><p class="text-sm text-slate-500">Patient Amount</p><p class="font-semibold">{{ number_format($invoice->patient_amount,2) }}</p></x-card><x-card><p class="text-sm text-slate-500">Paid</p><p class="font-semibold">{{ number_format($invoice->paid_amount,2) }}</p></x-card><x-card><p class="text-sm text-slate-500">Balance</p><p class="font-semibold">{{ number_format($invoice->balance_amount,2) }}</p></x-card></div>
    <x-card><div class="flex items-center justify-between"><h3 class="font-semibold">Invoice Items</h3><div class="flex gap-2"><a href="{{ route('billing.invoices.print',$invoice) }}" class="rounded-md border border-slate-300 px-3 py-2 text-sm dark:border-slate-700">Print</a><button wire:click="$set('showPaymentModal', true)" class="rounded-md bg-primary px-3 py-2 text-sm font-semibold text-white">Receive Payment</button></div></div><div class="mt-3 overflow-x-auto"><table class="min-w-full text-sm"><thead class="text-left text-xs uppercase text-slate-500"><tr><th class="px-3 py-2">Item</th><th class="px-3 py-2">Qty</th><th class="px-3 py-2">Gross</th><th class="px-3 py-2">Patient</th><th class="px-3 py-2">Insurance</th><th class="px-3 py-2">Status</th></tr></thead><tbody>@foreach($invoice->items as $item)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="px-3 py-2">{{ $item->description_snapshot ?? $item->description }}</td><td class="px-3 py-2">{{ $item->quantity }}</td><td class="px-3 py-2">{{ number_format($item->gross_amount ?: $item->total_amount,2) }}</td><td class="px-3 py-2">{{ number_format($item->patient_amount,2) }}</td><td class="px-3 py-2">{{ number_format($item->insurance_amount,2) }}</td><td class="px-3 py-2">{{ $item->status }}</td></tr>@endforeach</tbody></table></div></x-card>
    <x-card><h3 class="font-semibold">Payments and Receipts</h3><div class="mt-3 divide-y divide-slate-100 dark:divide-slate-800">@forelse($invoice->payments as $payment)<div class="flex justify-between py-3 text-sm"><span>{{ $payment->payment_number }} - {{ $payment->method?->name }}</span><span>{{ number_format($payment->amount,2) }}</span></div>@empty<p class="py-6 text-sm text-slate-500">Hakuna malipo.</p>@endforelse</div></x-card>
    @if($showPaymentModal)<div class="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/60 p-4"><div class="w-full max-w-lg rounded-md bg-white p-5 shadow-xl dark:bg-card-dark"><div class="mb-4 flex items-center justify-between"><h3 class="font-semibold">Receive Payment</h3><button wire:click="$set('showPaymentModal', false)"><x-lucide-x class="h-5 w-5" /></button></div><form wire:submit="receivePayment" class="space-y-4"><label class="block"><span class="text-sm">Method</span><select wire:model="payment_method_id" class="mt-1 w-full rounded-md border border-slate-300 bg-white px-3 py-2 dark:border-slate-700 dark:bg-slate-900"><option value="">Select</option>@foreach($methods as $method)<option value="{{ $method->id }}">{{ $method->name }}</option>@endforeach</select>@error('payment_method_id')<p class="text-xs text-red-600">{{ $message }}</p>@enderror</label><label class="block"><span class="text-sm">Amount</span><input wire:model="amount" class="mt-1 w-full rounded-md border border-slate-300 bg-white px-3 py-2 dark:border-slate-700 dark:bg-slate-900">@error('amount')<p class="text-xs text-red-600">{{ $message }}</p>@enderror</label><label class="block"><span class="text-sm">Reference</span><input wire:model="transaction_reference" class="mt-1 w-full rounded-md border border-slate-300 bg-white px-3 py-2 dark:border-slate-700 dark:bg-slate-900"></label><div class="flex justify-end gap-2"><button type="button" wire:click="$set('showPaymentModal', false)" class="rounded-md border border-slate-300 px-4 py-2 text-sm dark:border-slate-700">Cancel</button><button class="rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white">Confirm</button></div></form></div></div>@endif
</div>
