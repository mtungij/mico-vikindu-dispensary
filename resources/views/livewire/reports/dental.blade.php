<div class="space-y-4"><x-card><div class="flex flex-wrap gap-2">@foreach(['patients','diagnoses','procedures','tooth-findings','treatment-plans','orthodontics','dental-lab','materials','revenue','provider-workload'] as $t)<a href="{{ route('reports.dental.'.$t) }}" class="rounded-md px-3 py-2 text-sm {{ $type===$t?'bg-primary text-white':'bg-slate-100 dark:bg-slate-800' }}">{{ str($t)->replace('-',' ')->title() }}</a>@endforeach</div></x-card><x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-slate-500"><th class="p-2">Record</th><th>Patient</th><th>Status</th><th>Date</th></tr></thead><tbody>@foreach($rows as $row)<tr class="border-t border-slate-200 dark:border-slate-700"><td class="p-2">{{ $row->dental_encounter_number ?? $row->procedure_number ?? $row->plan_number ?? $row->diagnosis_name ?? $row->id }}</td><td>{{ $row->patient?->fullName() ?? '-' }}</td><td>{{ $row->status?->value ?? $row->status ?? '-' }}</td><td>{{ $row->created_at?->format('d/m/Y') }}</td></tr>@endforeach</tbody></table></div></x-card></div>
