<div class="space-y-6"><x-card class="grid gap-4 md:grid-cols-3"><div><p class="text-xs text-slate-500">Location</p><p class="font-semibold">{{ $count->location?->name }}</p></div><div><p class="text-xs text-slate-500">Status</p><p class="font-semibold">{{ $count->status?->value ?? $count->status }}</p></div><div><p class="text-xs text-slate-500">Items</p><p class="font-semibold">{{ $count->items->count() }}</p></div></x-card><x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Medicine</th><th>Batch</th><th>System</th><th>Counted</th><th>Variance</th></tr></thead><tbody>@foreach($count->items as $item)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3">{{ $item->medicine_id }}</td><td>{{ $item->medicine_batch_id }}</td><td>{{ $item->system_quantity }}</td><td>{{ $item->counted_quantity }}</td><td>{{ $item->variance_quantity }}</td></tr>@endforeach</tbody></table></div></x-card></div>
