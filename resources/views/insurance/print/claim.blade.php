<!doctype html><html><head><meta charset="utf-8"><title>{{ $claim->claim_number }}</title><style>body{font-family:Arial,sans-serif;color:#111827}table{width:100%;border-collapse:collapse}td,th{border:1px solid #d1d5db;padding:6px;font-size:12px}.muted{color:#6b7280}</style></head><body><h1>{{ $claim->facility?->name }}</h1><p class="muted">Facility claim form. Not an official electronic NHIF template unless verified and configured.</p><h2>{{ $claim->claim_number }}</h2><p>Patient: {{ $claim->patient?->first_name }} {{ $claim->patient?->last_name }} | Membership: {{ $claim->membership?->membership_number }} | Provider: {{ $claim->provider?->name }}</p><table><thead><tr><th>Description</th><th>Qty</th><th>Gross</th><th>Patient</th><th>Claimed</th></tr></thead><tbody>@foreach($claim->items as $item)<tr><td>{{ $item->description_snapshot }}</td><td>{{ $item->quantity }}</td><td>{{ number_format($item->gross_amount,2) }}</td><td>{{ number_format($item->patient_amount,2) }}</td><td>{{ number_format($item->claimed_amount,2) }}</td></tr>@endforeach</tbody></table><p>Total claimed: {{ number_format($claim->payer_claimed_amount,2) }}</p></body></html>
