<div class="space-y-6"><div class="flex justify-between"><div class="flex gap-2"><a href="{{ route('settings.observation.rooms') }}" class="rounded-md bg-white px-3 py-2 text-sm dark:bg-card-dark">Rooms</a><a href="{{ route('settings.observation.beds') }}" class="rounded-md bg-primary px-3 py-2 text-sm text-white">Beds</a></div><x-primary-button wire:click="create"><x-lucide-plus class="h-4 w-4" /> Bed</x-primary-button></div><x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Bed</th><th>Room</th><th>Type</th><th>Status</th><th>Cleaning</th><th class="text-right">Actions</th></tr></thead><tbody>@foreach($beds as $bed)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3 font-semibold">{{ $bed->name }}<div class="text-xs text-slate-500">{{ $bed->code }}</div></td><td>{{ $bed->room?->name }}</td><td>{{ $bed->bed_type?->value }}</td><td><x-badge>{{ $bed->status?->value }}</x-badge></td><td>{{ $bed->current_cleaning_status?->value }}</td><td class="text-right"><button wire:click="edit({{ $bed->id }})" class="rounded-md p-2 hover:bg-slate-100 dark:hover:bg-slate-800"><x-lucide-pencil class="h-4 w-4" /></button></td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $beds->links() }}</div></x-card><x-modal :show="$showModal" title="Bed" close="$set('showModal', false)"><form wire:submit="save" class="grid gap-3 md:grid-cols-2"><select wire:model="form.observation_room_id" class="rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-900"><option value="">Room</option>@foreach($rooms as $room)<option value="{{ $room->id }}">{{ $room->name }}</option>@endforeach</select><x-text-input wire:model="form.name" placeholder="Name" /><x-text-input wire:model="form.code" placeholder="Code" /><x-text-input wire:model="form.bed_type" placeholder="Bed type" /><x-text-input wire:model="form.hourly_rate" placeholder="Hourly fallback" /><x-text-input wire:model="form.daily_rate" placeholder="Daily fallback" /><x-primary-button>Save</x-primary-button></form></x-modal></div>
