<div class="space-y-6">
    <div class="flex justify-end"><x-primary-button wire:click="create"><x-lucide-plus class="h-4 w-4" /> Category</x-primary-button></div>
    <x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Name</th><th>Code</th><th>Tests</th><th>Active</th><th class="text-right">Actions</th></tr></thead><tbody>@foreach($categories as $category)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3 font-semibold">{{ $category->name }}</td><td>{{ $category->code }}</td><td>{{ $category->tests_count }}</td><td>{{ $category->is_active ? 'Yes' : 'No' }}</td><td class="text-right"><button wire:click="edit({{ $category->id }})" class="rounded-md p-2 hover:bg-slate-100 dark:hover:bg-slate-800"><x-lucide-pencil class="h-4 w-4" /></button></td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $categories->links() }}</div></x-card>
    <x-modal :show="$showModal" title="Laboratory Category" close="$set('showModal', false)" maxWidth="lg"><form wire:submit="save" class="space-y-3"><x-text-input wire:model="form.name" placeholder="Name" /><x-text-input wire:model="form.code" placeholder="Code" /><x-textarea wire:model="form.description" rows="3" placeholder="Description" /><label class="flex items-center gap-2 text-sm"><input type="checkbox" wire:model="form.is_active"> Active</label><div class="flex justify-end"><x-primary-button>Hifadhi</x-primary-button></div></form></x-modal>
</div>
