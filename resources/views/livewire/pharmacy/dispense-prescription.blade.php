<form wire:submit="dispense" class="grid gap-6 xl:grid-cols-[1fr_22rem]">
    <x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Medicine</th><th>Quantity</th><th>Substitution Reason</th><th>Unavailable / Declined</th></tr></thead><tbody>@foreach($form->lines as $i => $line)@php($itemId = $line['prescription_item_id'])<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3"><select wire:model="form.lines.{{ $i }}.medicine_id" class="w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-900">@foreach(\App\Models\Medicine::query()->forCurrentFacility()->where('is_active', true)->get() as $medicine)<option value="{{ $medicine->id }}">{{ $medicine->name }} ({{ $medicine->currentStock() }})</option>@endforeach</select></td><td><x-text-input wire:model="form.lines.{{ $i }}.quantity" /></td><td><x-text-input wire:model="form.lines.{{ $i }}.substitution_reason" /></td><td><x-text-input wire:model="terminalReasons.{{ $itemId }}" placeholder="Reason" /><button type="button" wire:click="declineItem({{ $itemId }})" class="mt-1 text-xs text-red-600">Mark unavailable</button></td></tr>@endforeach</tbody></table></div></x-card>
    <x-card><label class="text-sm">Location</label><select wire:model="form.stock_location_id" class="mt-1 w-full rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-900">@foreach($locations as $location)<option value="{{ $location->id }}">{{ $location->name }}</option>@endforeach</select><label class="mt-4 block text-sm">Override reason</label><x-textarea wire:model="form.override_reason" rows="3" /><x-primary-button class="mt-4 w-full justify-center">Confirm Dispensing</x-primary-button></x-card>
</form>
