<!DOCTYPE html><html><head><meta charset="utf-8"><title>{{ $admission->admission_number }}</title><style>body{font-family:sans-serif;color:#111827}.section{border-top:1px solid #111;padding:12px 0}.small{font-size:12px;color:#475569}@media print{button{display:none}}</style></head><body><button onclick="window.print()">Print</button><h2>{{ currentFacility()?->name }}</h2><h3>Observation Chart: {{ $admission->admission_number }}</h3><p>{{ $admission->patient?->full_name }} · {{ $admission->patient?->patient_number }} · Bed {{ $admission->bed?->code }}</p><div class="section"><strong>Reason</strong><p>{{ $admission->reason_for_admission }}</p></div><div class="section"><strong>Vitals</strong>@foreach($admission->observations as $o)<p class="small">{{ $o->recorded_at?->format('d M Y H:i') }} T {{ $o->temperature }} BP {{ $o->systolic_bp }}/{{ $o->diastolic_bp }} SpO2 {{ $o->oxygen_saturation }} {{ $o->notes }}</p>@endforeach</div><div class="section"><strong>Medication</strong>@foreach($admission->medicationAdministrations as $m)<p class="small">{{ $m->scheduled_at?->format('d M H:i') }} {{ $m->medicine_name_snapshot }} {{ $m->dose }} {{ $m->administration_status?->value }}</p>@endforeach</div><div class="section"><strong>Orders</strong>@foreach($admission->orders as $o)<p>{{ $o->order_type }}: {{ $o->instructions }}</p>@endforeach</div></body></html>
