<div class="space-y-6">
    <div class="flex gap-2"><a href="{{ route('pharmacy.returns.index') }}" class="rounded-md px-3 py-2 text-sm {{ $type === 'patient' ? 'bg-primary text-white' : 'bg-white dark:bg-card-dark' }}">Patient Returns</a><a href="{{ route('pharmacy.supplier-returns.index') }}" class="rounded-md px-3 py-2 text-sm {{ $type === 'supplier' ? 'bg-primary text-white' : 'bg-white dark:bg-card-dark' }}">Supplier Returns</a></div>
    <x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Return Number</th><th>Reason</th><th>Status</th><th>Date</th></tr></thead><tbody>@forelse($rows as $row)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3 font-semibold">{{ $row->return_number }}</td><td>{{ $row->reason }}</td><td><x-badge tone="blue">{{ $row->status?->value ?? $row->status }}</x-badge></td><td>{{ $row->created_at?->format('d M Y H:i') }}</td></tr>@empty<tr><td colspan="4" class="py-8 text-center text-slate-500">Hakuna returns.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $rows->links() }}</div></x-card>
</div>
