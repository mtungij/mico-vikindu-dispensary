<div class="space-y-4"><x-card><div class="grid gap-3 md:grid-cols-3"><div><p class="text-sm text-slate-500">Reference</p><p class="font-semibold">{{ $insurancePayment->payment_reference }}</p></div><div><p class="text-sm text-slate-500">Amount</p><p class="font-semibold">{{ number_format($insurancePayment->amount,2) }}</p></div><div><p class="text-sm text-slate-500">Unallocated</p><p class="font-semibold">{{ number_format($balance,2) }}</p></div></div></x-card><x-card><h3 class="font-semibold">Allocations</h3>@forelse($insurancePayment->allocations as $allocation)<p class="border-t border-slate-100 py-3 text-sm dark:border-slate-800">{{ $allocation->claim?->claim_number }} - {{ number_format($allocation->allocated_amount,2) }}</p>@empty<p class="py-6 text-sm text-slate-500">Hakuna allocations.</p>@endforelse</x-card></div>
