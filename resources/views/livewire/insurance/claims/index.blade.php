<div class="space-y-4">
    <x-card><div class="grid gap-3 md:grid-cols-3"><input wire:model.live.debounce.300ms="search" class="rounded-md border border-slate-300 bg-white px-3 py-2 text-sm dark:border-slate-700 dark:bg-slate-900" placeholder="Claim number or membership"><select wire:model.live="status" class="rounded-md border border-slate-300 bg-white px-3 py-2 text-sm dark:border-slate-700 dark:bg-slate-900"><option value="">All statuses</option>@foreach($statuses as $s)<option value="{{ $s }}">{{ str($s)->replace('_',' ')->title() }}</option>@endforeach</select><a href="{{ route('insurance.claims.reports.nhif') }}" class="inline-flex items-center justify-center gap-2 rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white"><x-lucide-file-text class="h-4 w-4" /> NHIF Claim Report</a></div></x-card>
    <x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="text-left text-xs uppercase text-slate-500"><tr><th class="px-3 py-2">Claim</th><th class="px-3 py-2">Patient</th><th class="px-3 py-2">Provider</th><th class="px-3 py-2">Claimed</th><th class="px-3 py-2">Outstanding</th><th class="px-3 py-2">Status</th></tr></thead><tbody>@forelse($claims as $claim)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="px-3 py-2"><a class="font-semibold text-primary" href="{{ route('insurance.claims.show',$claim) }}">{{ $claim->claim_number }}</a></td><td class="px-3 py-2">{{ $claim->patient?->first_name }} {{ $claim->patient?->last_name }}</td><td class="px-3 py-2">{{ $claim->provider?->name }}</td><td class="px-3 py-2">{{ number_format($claim->payer_claimed_amount,2) }}</td><td class="px-3 py-2">{{ number_format($claim->outstanding_amount,2) }}</td><td class="px-3 py-2">{{ str($claim->status)->replace('_',' ')->title() }}</td></tr>@empty<tr><td colspan="6" class="px-3 py-8 text-center text-slate-500">Hakuna claims.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $claims->links() }}</div></x-card>
</div>
