<div class="space-y-6" wire:poll.30s>
    <div class="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div class="flex flex-wrap gap-2">@foreach(['awaiting_payment'=>'Awaiting Payment','awaiting_sample'=>'Awaiting Sample','processing'=>'Processing','completed'=>'Completed'] as $key=>$label)<button wire:click="$set('tab','{{ $key }}')" class="rounded-md px-3 py-2 text-sm {{ $tab===$key?'bg-primary text-white':'bg-slate-100 dark:bg-slate-800' }}">{{ $label }}</button>@endforeach</div><x-text-input wire:model.live.debounce.300ms="search" placeholder="Tafuta..." /></div>
    <x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Order</th><th>Patient</th><th>Tests</th><th>Priority</th><th>Payment</th><th>Status</th><th>Ordered</th><th class="text-right">Actions</th></tr></thead><tbody>@foreach($orders as $order)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3 font-semibold">{{ $order->order_number }}</td><td>{{ $order->patient?->fullName() }}<div class="text-xs text-slate-500">{{ $order->patient?->patient_number }}</div></td><td>{{ $order->items->pluck('test_name_snapshot')->implode(', ') }}</td><td>{{ $order->priority }}</td><td>{{ $order->payment_status->value }}</td><td>{{ $order->status->value }}</td><td>{{ $order->ordered_at?->diffForHumans() }}</td><td class="text-right"><a href="{{ route('laboratory.orders.show', $order) }}" class="rounded-md p-2 hover:bg-slate-100 dark:hover:bg-slate-800"><x-lucide-eye class="h-4 w-4" /></a>@if($order->payment_status->value !== 'pending' || auth()->user()->can('laboratory.override-payment'))<button wire:click="openCollect({{ $order->id }})" class="rounded-md p-2 hover:bg-slate-100 dark:hover:bg-slate-800"><x-lucide-test-tube class="h-4 w-4" /></button><a href="{{ route('laboratory.results.entry', $order) }}" class="rounded-md p-2 hover:bg-slate-100 dark:hover:bg-slate-800"><x-lucide-clipboard-check class="h-4 w-4" /></a>@endif</td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $orders->links() }}</div></x-card>
    <x-modal :show="$showCollectModal" title="Collect Sample" close="$set('showCollectModal', false)" maxWidth="2xl"><form wire:submit="collectAndAccept" class="space-y-3"><p class="text-sm text-slate-500">Sample number itatengenezwa server-side.</p><x-text-input type="datetime-local" wire:model="sampleForm.collected_at" /><x-text-input wire:model="sampleForm.volume_collected" placeholder="Volume" /><x-text-input wire:model="sampleForm.volume_unit" placeholder="Unit" /><x-text-input wire:model="sampleForm.collection_location" placeholder="Location" /><x-textarea wire:model="sampleForm.collection_notes" rows="3" placeholder="Notes" /><div class="flex justify-end"><x-primary-button>Collect and Accept</x-primary-button></div></form></x-modal>
</div>
