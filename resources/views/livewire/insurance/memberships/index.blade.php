<x-card><input wire:model.live.debounce.300ms="search" class="mb-4 w-full rounded-md border border-slate-300 bg-white px-3 py-2 text-sm dark:border-slate-700 dark:bg-slate-900" placeholder="Search membership number"><div class="overflow-x-auto"><table class="min-w-full text-sm"><tbody>@forelse($memberships as $membership)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="px-3 py-3"><a class="font-semibold text-primary" href="{{ route('insurance.memberships.show',$membership) }}">{{ $membership->membership_number }}</a></td><td class="px-3 py-3">{{ $membership->patient?->first_name }} {{ $membership->patient?->last_name }}</td><td class="px-3 py-3">{{ $membership->provider?->name }}</td><td class="px-3 py-3">{{ str($membership->verification_status)->replace('_',' ')->title() }}</td></tr>@empty<tr><td class="px-3 py-8 text-center text-slate-500">Hakuna memberships.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $memberships->links() }}</div></x-card>
