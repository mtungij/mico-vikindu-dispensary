<x-card><div class="space-y-4">@forelse($schedules as $schedule)<div class="rounded-md border p-4 dark:border-slate-700"><h3 class="font-semibold">{{ $schedule->name }}</h3><p class="text-sm text-slate-500">{{ $schedule->code }} · {{ $schedule->target_group }}</p><div class="mt-3 grid gap-2">@foreach($schedule->items as $item)<div class="text-sm">{{ $item->dose_name }} · {{ $item->vaccine->name }} · day {{ $item->recommended_age_days }}</div>@endforeach</div></div>@empty<x-empty-state icon="calendar" title="No immunization schedules" />@endforelse</div></x-card>
