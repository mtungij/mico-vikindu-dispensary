<div class="space-y-6"><div class="grid gap-4 md:grid-cols-4"><x-card><p class="text-sm text-slate-500">Registered Today</p><p class="mt-2 text-2xl font-semibold">{{ $patientsToday }}</p></x-card><x-card><p class="text-sm text-slate-500">New Today</p><p class="mt-2 text-2xl font-semibold">{{ $newPatientsToday }}</p></x-card><x-card><p class="text-sm text-slate-500">Awaiting Payment</p><p class="mt-2 text-2xl font-semibold">{{ $awaitingPayment }}</p></x-card><x-card><p class="text-sm text-slate-500">Active Visits</p><p class="mt-2 text-2xl font-semibold">{{ $activeVisits }}</p></x-card></div><div class="flex gap-2"><a href="{{ route('patients.index') }}" class="inline-flex items-center gap-2 rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white"><x-lucide-user-plus class="h-4 w-4" /> Sajili Mgonjwa</a><a href="{{ route('reception.queue') }}" class="inline-flex items-center gap-2 rounded-md border border-slate-300 px-4 py-2 text-sm font-semibold"><x-lucide-list-ordered class="h-4 w-4" /> Foleni</a></div><x-card><h3 class="font-semibold">Reception Queue</h3><div class="mt-4 overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th>Queue</th><th>Patient</th><th>Visit</th><th>Department</th><th>Priority</th><th>Status</th></tr></thead><tbody>@foreach($queues as $queue)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3">{{ $queue->queue_number }}</td><td>{{ $queue->patient->fullName() }}</td><td>{{ $queue->visit->visit_number }}</td><td>{{ $queue->department->name }}</td><td>{{ $queue->priority->value }}</td><td>{{ $queue->queue_status }}</td></tr>@endforeach</tbody></table></div></x-card></div>
