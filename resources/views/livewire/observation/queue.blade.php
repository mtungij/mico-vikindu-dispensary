<div class="space-y-6">
    <div class="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div class="flex flex-wrap gap-2">@foreach(['admitted'=>'Admitted','awaiting_payment'=>'Awaiting Payment','awaiting_bed'=>'Awaiting Bed','ready'=>'Ready','referred'=>'Referred','discharged_today'=>'Discharged Today','all'=>'All'] as $key=>$label)<button wire:click="$set('tab','{{ $key }}')" class="rounded-md px-3 py-2 text-sm {{ $tab===$key?'bg-primary text-white':'bg-white dark:bg-card-dark' }}">{{ $label }}</button>@endforeach</div><x-text-input wire:model.live.debounce.300ms="search" placeholder="Tafuta..." /></div>
    <x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Admission</th><th>Patient</th><th>Bed</th><th>Payer</th><th>Status</th><th>Acuity</th><th>Admitted</th><th class="text-right">Actions</th></tr></thead><tbody>@forelse($admissions as $admission)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3 font-semibold">{{ $admission->admission_number }}</td><td>{{ $admission->patient?->full_name }}<div class="text-xs text-slate-500">{{ $admission->patient?->patient_number }}</div></td><td>{{ $admission->bed?->code ?? 'Awaiting' }}</td><td>{{ $admission->payer_type?->value ?? $admission->payer_type }}</td><td><x-badge>{{ $admission->status?->value }}</x-badge></td><td>{{ $admission->acuity_level }}</td><td>{{ $admission->admitted_at?->diffForHumans() }}</td><td class="text-right"><a href="{{ route('observation.admissions.show',$admission) }}" class="rounded-md p-2 hover:bg-slate-100 dark:hover:bg-slate-800"><x-lucide-eye class="h-4 w-4" /></a></td></tr>@empty<tr><td colspan="8" class="py-8 text-center text-slate-500">Hakuna admissions.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $admissions->links() }}</div></x-card>
</div>
