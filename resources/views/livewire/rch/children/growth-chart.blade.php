<div class="space-y-5"><x-card><h2 class="font-semibold">{{ $rchChild->patient->fullName() }} Growth Chart</h2><p class="text-sm text-slate-500">Imported WHO/reference rows are used only when available; no demo values are treated as authoritative.</p></x-card><x-card><div class="grid gap-3">@forelse($measurements as $m)<div class="grid grid-cols-4 rounded-md border p-3 text-sm dark:border-slate-700"><span>{{ $m->measured_at?->toDateString() }}</span><span>{{ $m->age_in_days }} days</span><span>{{ $m->weight_kg }} kg</span><span>{{ $m->length_height_cm }} cm</span></div>@empty<x-empty-state icon="chart-no-axes-combined" title="No growth history" />@endforelse</div></x-card></div>
