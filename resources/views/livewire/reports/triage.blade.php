<x-card><div class="mb-4 text-right"><a href="{{ route('reports.triage.export') }}" class="rounded-md bg-primary px-3 py-2 text-sm text-white">CSV</a></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Date</th><th>Patient</th><th>Level</th><th>Vitals</th><th>Nurse</th></tr></thead><tbody>@foreach($rows as $row)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3">{{ $row->assessed_at?->format('d/m/Y H:i') }}</td><td>{{ $row->patient?->fullName() }}</td><td>{{ $row->triage_level->value }}</td><td>BP {{ $row->systolic_bp }}/{{ $row->diastolic_bp }} · SpO2 {{ $row->oxygen_saturation }}</td><td>{{ $row->assessor?->name }}</td></tr>@endforeach</tbody></table></div></x-card>
