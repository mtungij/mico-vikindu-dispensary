<x-card><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="p-3">Child</th><th>Date</th><th>Status</th><th>Referral</th></tr></thead><tbody>@forelse($assessments as $a)<tr class="border-t dark:border-slate-700"><td class="p-3">{{ $a->child->patient->fullName() }}</td><td>{{ $a->assessment_date?->toDateString() }}</td><td>{{ $a->overall_nutrition_status }}</td><td>{{ $a->referral_required ? 'Yes' : 'No' }}</td></tr>@empty<tr><td colspan="4" class="p-8"><x-empty-state icon="heart-pulse" title="No nutrition assessments" /></td></tr>@endforelse</tbody></table><div class="mt-4">{{ $assessments->links() }}</div></x-card>
