<div class="space-y-5"><x-card><h2 class="font-semibold">{{ $rchChild->patient->fullName() }} Immunization Card</h2><p class="text-sm text-slate-500">{{ $rchChild->child_rch_number }}</p></x-card><x-card><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="p-3">Vaccine</th><th>Dose</th><th>Date</th><th>Batch</th></tr></thead><tbody>@forelse($rchChild->immunizations as $record)<tr class="border-t dark:border-slate-700"><td class="p-3">{{ $record->vaccine->name }}</td><td>{{ $record->dose_name_snapshot }}</td><td>{{ $record->administration_date?->toDateString() }}</td><td>{{ $record->batch_number_snapshot ?? '-' }}</td></tr>@empty<tr><td colspan="4" class="p-8"><x-empty-state icon="syringe" title="No immunizations recorded" /></td></tr>@endforelse</tbody></table></x-card></div>
