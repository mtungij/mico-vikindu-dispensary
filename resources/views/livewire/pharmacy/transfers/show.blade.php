<div class="space-y-6">
    <div class="flex justify-end gap-2"><x-secondary-button wire:click="dispatchTransfer"><x-lucide-send class="h-4 w-4" /> Dispatch</x-secondary-button><x-primary-button wire:click="receive"><x-lucide-check class="h-4 w-4" /> Receive</x-primary-button></div>
    <x-card class="grid gap-4 md:grid-cols-3"><div><p class="text-xs text-slate-500">From</p><p class="font-semibold">{{ $transfer->fromLocation?->name }}</p></div><div><p class="text-xs text-slate-500">To</p><p class="font-semibold">{{ $transfer->toLocation?->name }}</p></div><div><p class="text-xs text-slate-500">Status</p><p class="font-semibold">{{ $transfer->status?->value ?? $transfer->status }}</p></div></x-card>
    <x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Medicine</th><th>Batch</th><th>Requested</th><th>Dispatched</th><th>Received</th></tr></thead><tbody>@foreach($transfer->items as $item)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3 font-semibold">{{ $item->medicine?->name }}</td><td>{{ $item->batch?->batch_number }}</td><td>{{ $item->quantity_requested }}</td><td>{{ $item->quantity_dispatched }}</td><td>{{ $item->quantity_received }}</td></tr>@endforeach</tbody></table></div></x-card>
</div>
