<div class="space-y-4">
    <x-card><div class="grid gap-3 md:grid-cols-3"><input wire:model.live.debounce.300ms="search" class="rounded-md border border-slate-300 bg-white px-3 py-2 text-sm dark:border-slate-700 dark:bg-slate-900" placeholder="Tafuta invoice au mgonjwa"><select wire:model.live="tab" class="rounded-md border border-slate-300 bg-white px-3 py-2 text-sm dark:border-slate-700 dark:bg-slate-900"><option value="awaiting">Awaiting Payment</option><option value="partial">Partially Paid</option><option value="paid_today">Paid Today</option><option value="all">All</option></select><a href="{{ route('cashier.dashboard') }}" class="inline-flex items-center justify-center gap-2 rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white"><x-lucide-banknote class="h-4 w-4" /> Cashier</a></div></x-card>
    <x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="text-left text-xs uppercase text-slate-500"><tr><th class="px-3 py-2">Invoice</th><th class="px-3 py-2">Patient</th><th class="px-3 py-2">Payer</th><th class="px-3 py-2">Patient Amount</th><th class="px-3 py-2">Insurance</th><th class="px-3 py-2">Paid</th><th class="px-3 py-2">Balance</th><th class="px-3 py-2">Status</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="px-3 py-2"><a class="font-semibold text-primary" href="{{ route('billing.invoices.show',$invoice) }}">{{ $invoice->invoice_number }}</a></td><td class="px-3 py-2">{{ $invoice->patient?->first_name }} {{ $invoice->patient?->last_name }}</td><td class="px-3 py-2">{{ $invoice->payer_type?->value ?? $invoice->payer_type }}</td><td class="px-3 py-2">{{ number_format($invoice->patient_amount,2) }}</td><td class="px-3 py-2">{{ number_format($invoice->insurance_amount,2) }}</td><td class="px-3 py-2">{{ number_format($invoice->paid_amount,2) }}</td><td class="px-3 py-2">{{ number_format($invoice->balance_amount,2) }}</td><td class="px-3 py-2">{{ str($invoice->payment_status)->title() }}</td></tr>@empty<tr><td colspan="8" class="px-3 py-8 text-center text-slate-500">Hakuna invoice kwenye foleni.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $invoices->links() }}</div></x-card>
</div>
