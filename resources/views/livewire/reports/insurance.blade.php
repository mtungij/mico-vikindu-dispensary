<x-card><h3 class="font-semibold">{{ str($type)->replace('-',' ')->title() }}</h3><div class="mt-3 overflow-x-auto"><table class="min-w-full text-sm"><tbody>@forelse($claims as $claim)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="px-3 py-3">{{ $claim->claim_number }}</td><td class="px-3 py-3">{{ $claim->provider?->name }}</td><td class="px-3 py-3">{{ number_format($claim->payer_claimed_amount,2) }}</td><td class="px-3 py-3">{{ $claim->status }}</td></tr>@empty<tr><td class="px-3 py-8 text-center text-slate-500">Hakuna data ya report.</td></tr>@endforelse</tbody></table></div></x-card>
