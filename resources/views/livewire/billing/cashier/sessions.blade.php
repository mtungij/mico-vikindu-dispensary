<div class="space-y-4"><x-card><div class="flex gap-2"><button wire:click="$set('showOpen', true)" class="rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white">Open Session</button><button wire:click="$set('showClose', true)" class="rounded-md border border-slate-300 px-4 py-2 text-sm dark:border-slate-700">Close Session</button></div></x-card><x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><tbody>@foreach($sessions as $session)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="px-3 py-3"><a class="font-semibold text-primary" href="{{ route('cashier.sessions.show', $session) }}">{{ $session->session_number }}</a></td><td class="px-3 py-3">{{ $session->opened_at?->format('Y-m-d H:i') }}</td><td class="px-3 py-3">{{ $session->status }}</td><td class="px-3 py-3">{{ number_format($session->variance ?? 0,2) }}</td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $sessions->links() }}</div></x-card>@if($showOpen || $showClose)<div class="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/60 p-4"><div class="w-full max-w-md rounded-md bg-white p-5 dark:bg-card-dark"><h3 class="mb-4 font-semibold">{{ $showOpen ? 'Open Session' : 'Close Session' }}</h3>@if($showOpen)<input wire:model="opening_float" class="mb-3 w-full rounded-md border px-3 py-2 dark:border-slate-700 dark:bg-slate-900" placeholder="Opening float"><textarea wire:model="notes" class="mb-3 w-full rounded-md border px-3 py-2 dark:border-slate-700 dark:bg-slate-900" placeholder="Notes"></textarea><button wire:click="open" class="rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white">Open</button>@else<input wire:model="declared_cash" class="mb-3 w-full rounded-md border px-3 py-2 dark:border-slate-700 dark:bg-slate-900" placeholder="Declared cash"><textarea wire:model="notes" class="mb-3 w-full rounded-md border px-3 py-2 dark:border-slate-700 dark:bg-slate-900" placeholder="Notes"></textarea><button wire:click="close" class="rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white">Close</button>@endif<button wire:click="closeModal" class="ml-2 rounded-md border px-4 py-2 text-sm dark:border-slate-700">Cancel</button></div></div>@endif</div>
