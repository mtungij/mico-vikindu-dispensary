<div class="grid gap-4 lg:grid-cols-2"><x-card><h3 class="font-semibold">Membership</h3><dl class="mt-3 space-y-2 text-sm"><div><dt class="text-slate-500">Patient</dt><dd>{{ $membership->patient?->first_name }} {{ $membership->patient?->last_name }}</dd></div><div><dt class="text-slate-500">Provider</dt><dd>{{ $membership->provider?->name }}</dd></div><div><dt class="text-slate-500">Scheme</dt><dd>{{ $membership->scheme?->name }}</dd></div><div><dt class="text-slate-500">Status</dt><dd>{{ str($membership->verification_status)->replace('_',' ')->title() }}</dd></div></dl></x-card><x-card><h3 class="font-semibold">Eligibility</h3><p class="mt-2 text-sm {{ $summary['eligible'] ? 'text-emerald-600' : 'text-red-600' }}">{{ $summary['eligible'] ? 'Eligible' : 'Not eligible' }}</p>@foreach($summary['blocking_errors'] as $error)<p class="mt-2 text-sm text-red-600">{{ $error }}</p>@endforeach</x-card></div>
