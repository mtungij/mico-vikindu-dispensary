<div class="space-y-6"><div class="flex flex-wrap gap-2">@foreach(['admissions','occupancy','nursing','medication-administration','discharges','bed-cleaning','revenue','length-of-stay','procedures','referrals'] as $name)<a href="{{ route('reports.observation.'.$name) }}" class="rounded-md px-3 py-2 text-sm {{ $type===$name?'bg-primary text-white':'bg-white dark:bg-card-dark' }}">{{ str($name)->replace('-',' ')->title() }}</a>@endforeach</div><x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Record</th><th>Status</th><th>Date</th></tr></thead><tbody>@forelse($rows as $row)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3 font-semibold">{{ $row->admission_number ?? $row->bed?->code ?? $row->medicine_name_snapshot ?? $row->patient?->full_name ?? ('#'.$row->id) }}</td><td>{{ $row->status?->value ?? $row->status ?? $row->administration_status?->value ?? '' }}</td><td>{{ ($row->admitted_at ?? $row->recorded_at ?? $row->scheduled_at ?? $row->requested_at ?? $row->created_at)?->format('d M Y H:i') }}</td></tr>@empty<tr><td colspan="3" class="py-8 text-center text-slate-500">Hakuna data.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ method_exists($rows,'links') ? $rows->links() : '' }}</div></x-card></div>
