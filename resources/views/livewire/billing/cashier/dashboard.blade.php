<div class="space-y-4"><div class="grid gap-4 md:grid-cols-3"><x-card><p class="text-sm text-slate-500">Current Session</p><p class="font-semibold">{{ $session?->session_number ?? 'No open session' }}</p></x-card><x-card><p class="text-sm text-slate-500">Expected Cash</p><p class="font-semibold">{{ number_format($expected,2) }}</p></x-card><x-card><a href="{{ route('cashier.sessions.index') }}" class="inline-flex rounded-md bg-primary px-4 py-2 text-sm font-semibold text-white">Sessions</a></x-card></div><x-card><h3 class="font-semibold">My Payments Today</h3>@forelse($payments as $payment)<div class="flex justify-between border-t border-slate-100 py-3 text-sm dark:border-slate-800"><span>{{ $payment->payment_number }} - {{ $payment->method?->name }}</span><span>{{ number_format($payment->amount,2) }}</span></div>@empty<p class="py-6 text-sm text-slate-500">Hakuna malipo leo.</p>@endforelse</x-card></div>
