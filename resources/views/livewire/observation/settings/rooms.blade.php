<div class="space-y-6"><div class="flex justify-between"><div class="flex gap-2"><a href="{{ route('settings.observation.rooms') }}" class="rounded-md bg-primary px-3 py-2 text-sm text-white">Rooms</a><a href="{{ route('settings.observation.beds') }}" class="rounded-md bg-white px-3 py-2 text-sm dark:bg-card-dark">Beds</a></div><x-primary-button wire:click="create"><x-lucide-plus class="h-4 w-4" /> Room</x-primary-button></div><x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Room</th><th>Type</th><th>Capacity</th><th>Beds</th><th>Status</th><th class="text-right">Actions</th></tr></thead><tbody>@foreach($rooms as $room)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3 font-semibold">{{ $room->name }}<div class="text-xs text-slate-500">{{ $room->code }}</div></td><td>{{ $room->room_type?->value }}</td><td>{{ $room->capacity }}</td><td>{{ $room->beds_count }}</td><td><x-badge>{{ $room->is_active ? 'active' : 'inactive' }}</x-badge></td><td class="text-right"><button wire:click="edit({{ $room->id }})" class="rounded-md p-2 hover:bg-slate-100 dark:hover:bg-slate-800"><x-lucide-pencil class="h-4 w-4" /></button></td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $rooms->links() }}</div></x-card><x-modal :show="$showModal" title="Observation Room" close="$set('showModal', false)"><form wire:submit="save" class="grid gap-3 md:grid-cols-2"><x-text-input wire:model="form.name" placeholder="Name" /><x-text-input wire:model="form.code" placeholder="Code" /><x-text-input wire:model="form.room_type" placeholder="Room type" /><x-text-input wire:model="form.capacity" placeholder="Capacity" /><select wire:model="form.department_id" class="rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-900"><option value="">Department</option>@foreach($departments as $department)<option value="{{ $department->id }}">{{ $department->name }}</option>@endforeach</select><label class="flex items-center gap-2 text-sm"><input type="checkbox" wire:model="form.isolation_room"> Isolation</label><x-textarea wire:model="form.notes" class="md:col-span-2" placeholder="Notes" /><x-primary-button>Save</x-primary-button></form></x-modal></div>
