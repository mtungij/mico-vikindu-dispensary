<x-card><input wire:model.live.debounce.300ms="search" class="mb-4 w-full rounded-md border border-slate-300 bg-white px-3 py-2 text-sm dark:border-slate-700 dark:bg-slate-900" placeholder="Search invoice"><div class="overflow-x-auto"><table class="min-w-full text-sm"><tbody>@forelse($invoices as $invoice)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="px-3 py-3"><a class="font-semibold text-primary" href="{{ route('billing.invoices.show',$invoice) }}">{{ $invoice->invoice_number }}</a></td><td class="px-3 py-3">{{ $invoice->patient?->first_name }} {{ $invoice->patient?->last_name }}</td><td class="px-3 py-3">{{ number_format($invoice->total_amount,2) }}</td><td class="px-3 py-3">{{ number_format($invoice->balance_amount,2) }}</td><td class="px-3 py-3">{{ $invoice->payment_status }}</td></tr>@empty<tr><td class="px-3 py-8 text-center text-slate-500">Hakuna invoices.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $invoices->links() }}</div></x-card>
