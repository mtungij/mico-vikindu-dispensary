<div class="space-y-4"><x-card><div class="grid gap-3 md:grid-cols-4"><div><p class="text-sm text-slate-500">Batch</p><p class="font-semibold">{{ $claimBatch->batch_number }}</p></div><div><p class="text-sm text-slate-500">Claims</p><p class="font-semibold">{{ $claimBatch->claims_count }}</p></div><div><p class="text-sm text-slate-500">Total Claimed</p><p class="font-semibold">{{ number_format($claimBatch->total_claimed_amount,2) }}</p></div><div><p class="text-sm text-slate-500">Status</p><p class="font-semibold">{{ $claimBatch->status }}</p></div></div></x-card><x-card><h3 class="font-semibold">Claims</h3>@foreach($claimBatch->claims as $claim)<a class="block border-t border-slate-100 py-3 text-sm dark:border-slate-800" href="{{ route('insurance.claims.show',$claim) }}">{{ $claim->claim_number }} - {{ $claim->patient?->first_name }} {{ $claim->patient?->last_name }}</a>@endforeach</x-card></div>
