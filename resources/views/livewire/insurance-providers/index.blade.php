<div class="space-y-6"><div class="flex justify-end"><x-primary-button wire:click="create"><x-lucide-plus class="h-4 w-4" /> Add Provider</x-primary-button></div><x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Name</th><th>Code</th><th>Type</th><th>Status</th></tr></thead><tbody>@foreach($providers as $provider)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3 font-semibold">{{ $provider->name }}</td><td>{{ $provider->code }}</td><td>{{ $provider->provider_type->label() }}</td><td><x-badge :tone="$provider->is_active?'success':'danger'">{{ $provider->is_active?'Active':'Inactive' }}</x-badge></td></tr>@endforeach</tbody></table></div>{{ $providers->links() }}</x-card><x-modal :show="$showModal" title="Insurance Provider" close="$set('showModal', false)"><form wire:submit="save" class="space-y-4"><x-text-input wire:model="name" placeholder="Name" /><x-text-input wire:model="code" placeholder="Code" /><x-select-input wire:model="provider_type">@foreach($types as $type)<option value="{{ $type->value }}">{{ $type->label() }}</option>@endforeach</x-select-input><div class="flex justify-end"><x-primary-button>Save</x-primary-button></div></form></x-modal></div>
