<div class="space-y-5"><x-card><h2 class="font-semibold">{{ $pregnancy->pregnancy_number }} Risk Assessment</h2><p class="text-sm text-slate-500">Current level: {{ $pregnancy->risk_level ?? 'low' }}</p></x-card><x-card><div class="grid gap-3 md:grid-cols-2">@foreach($riskTypes as $type)<label class="flex items-center gap-2 text-sm"><x-checkbox wire:model="codes" value="{{ $type->code }}" /> {{ $type->name }} <span class="text-xs text-slate-500">({{ $type->severity }})</span></label>@endforeach</div><x-primary-button class="mt-4" wire:click="assess">Save Assessment</x-primary-button></x-card></div>
