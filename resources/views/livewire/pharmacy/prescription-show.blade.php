<div class="space-y-6">
    <x-card><div class="grid gap-3 md:grid-cols-4"><div><p class="text-xs text-slate-500">Patient</p><p class="font-semibold">{{ $prescription->patient?->fullName() }}</p></div><div><p class="text-xs text-slate-500">Allergies</p><p>{{ $prescription->patient?->known_allergies ?: '-' }}</p></div><div><p class="text-xs text-slate-500">Payment</p><p>{{ $prescription->visit?->invoice?->invoice_status?->value ?? 'not_required' }}</p></div><div class="text-right"><a href="{{ route('pharmacy.prescriptions.dispense',$prescription) }}" class="inline-flex rounded-md bg-primary px-3 py-2 text-sm text-white">Dispense</a></div></div></x-card>
    <x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Medicine</th><th>Dose</th><th>Qty</th><th>Dispensed</th><th>Remaining</th><th>Stock</th><th>Status</th></tr></thead><tbody>@foreach($prescription->items as $item)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3 font-semibold">{{ $item->medicine?->name ?? $item->medication_name }}<div class="text-xs text-slate-500">{{ $item->generic_name }} {{ $item->strength }}</div></td><td>{{ $item->dose }} {{ $item->route }} {{ $item->frequency }}</td><td>{{ $item->quantity }}</td><td>{{ $item->dispensed_quantity }}</td><td>{{ $item->remaining_quantity ?? $item->quantity }}</td><td>{{ $item->medicine?->currentStock() ?? '-' }}</td><td>{{ $item->dispensing_status ?? $item->status }}</td></tr>@endforeach</tbody></table></div></x-card>
</div>
