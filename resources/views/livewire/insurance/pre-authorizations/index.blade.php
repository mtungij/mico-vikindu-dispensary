<x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><tbody>@forelse($authorizations as $auth)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="px-3 py-3"><a class="font-semibold text-primary" href="{{ route('insurance.pre-authorizations.show',$auth) }}">{{ $auth->authorization_number ?? 'Draft #'.$auth->id }}</a></td><td class="px-3 py-3">{{ $auth->patient?->first_name }} {{ $auth->patient?->last_name }}</td><td class="px-3 py-3">{{ $auth->authorization_type }}</td><td class="px-3 py-3">{{ $auth->status }}</td></tr>@empty<tr><td class="px-3 py-8 text-center text-slate-500">Hakuna pre-authorizations.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $authorizations->links() }}</div></x-card>
