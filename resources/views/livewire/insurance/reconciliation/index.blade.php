<x-card><h3 class="font-semibold">Outstanding Claims</h3><div class="mt-3 divide-y divide-slate-100 dark:divide-slate-800">@forelse($claims as $claim)<a href="{{ route('insurance.claims.show',$claim) }}" class="flex justify-between py-3 text-sm"><span>{{ $claim->claim_number }} - {{ $claim->provider?->name }}</span><span>{{ number_format($claim->outstanding_amount,2) }}</span></a>@empty<p class="py-6 text-sm text-slate-500">Hakuna outstanding claims.</p>@endforelse</div></x-card>
