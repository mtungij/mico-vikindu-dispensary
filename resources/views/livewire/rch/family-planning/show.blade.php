<div class="space-y-5"><x-card><div class="flex justify-between"><div><h2 class="font-semibold">{{ $familyPlanningClient->patient->fullName() }}</h2><p class="text-sm text-slate-500">{{ $familyPlanningClient->fp_client_number }} · {{ $familyPlanningClient->currentMethod?->name ?? 'No active method' }}</p></div><a class="rounded-md bg-primary px-3 py-2 text-sm text-white" href="{{ route('rch.family-planning.visit',$familyPlanningClient) }}">Record Visit</a></div></x-card><x-card><h3 class="font-semibold">Method History</h3><div class="mt-3 space-y-2">@forelse($familyPlanningClient->methodEpisodes as $episode)<div class="rounded-md border p-3 text-sm dark:border-slate-700">{{ $episode->method->name }} · {{ $episode->status }} · {{ $episode->started_at?->toDateString() }}</div>@empty<x-empty-state icon="calendar" title="No method episodes" />@endforelse</div></x-card></div>
