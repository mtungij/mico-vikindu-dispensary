<div class="space-y-6">
    <div class="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><x-text-input wire:model.live.debounce.300ms="search" placeholder="Tafuta dawa..." /><x-primary-button wire:click="create"><x-lucide-plus class="h-4 w-4" /> Dawa</x-primary-button></div>
    <x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Medicine</th><th>Generic</th><th>Strength</th><th>Unit</th><th>Stock</th><th>Reorder</th><th>Status</th><th class="text-right">Actions</th></tr></thead><tbody>@foreach($medicines as $medicine)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3 font-semibold">{{ $medicine->name }}<div class="text-xs text-slate-500">{{ $medicine->code }}</div></td><td>{{ $medicine->generic?->name }}</td><td>{{ $medicine->strength }}</td><td>{{ $medicine->dispensingUnit?->symbol }}</td><td>{{ $medicine->batches_sum_available_quantity ?? 0 }}</td><td>{{ $medicine->reorder_level }}</td><td>{{ $medicine->is_active ? 'Active' : 'Inactive' }}</td><td class="text-right"><a href="{{ route('pharmacy.medicines.stock-card',$medicine) }}" class="rounded-md p-2 hover:bg-slate-100 dark:hover:bg-slate-800"><x-lucide-clipboard-list class="h-4 w-4" /></a><button wire:click="edit({{ $medicine->id }})" class="rounded-md p-2 hover:bg-slate-100 dark:hover:bg-slate-800"><x-lucide-pencil class="h-4 w-4" /></button></td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $medicines->links() }}</div></x-card>
    <x-modal :show="$showModal" title="Medicine" close="$set('showModal', false)" maxWidth="3xl"><form wire:submit="save" class="grid gap-3 md:grid-cols-2"><x-text-input wire:model="form.name" placeholder="Name" /><x-text-input wire:model="form.code" placeholder="Code" /><x-text-input wire:model="form.strength" placeholder="Strength" /><x-text-input wire:model="form.default_dispensing_price" placeholder="Default price" /><select wire:model="form.purchase_unit_id" class="rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-900"><option value="">Purchase unit</option>@foreach($units as $unit)<option value="{{ $unit->id }}">{{ $unit->name }}</option>@endforeach</select><select wire:model="form.dispensing_unit_id" class="rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-900"><option value="">Dispensing unit</option>@foreach($units as $unit)<option value="{{ $unit->id }}">{{ $unit->name }}</option>@endforeach</select><select wire:model="form.generic_medicine_id" class="rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-900"><option value="">Generic</option>@foreach($generics as $generic)<option value="{{ $generic->id }}">{{ $generic->name }}</option>@endforeach</select><select wire:model="form.dosage_form_id" class="rounded-md border-slate-300 dark:border-slate-700 dark:bg-slate-900"><option value="">Dosage form</option>@foreach($forms as $df)<option value="{{ $df->id }}">{{ $df->name }}</option>@endforeach</select><div class="md:col-span-2 flex justify-end"><x-primary-button>Save</x-primary-button></div></form></x-modal>
</div>
