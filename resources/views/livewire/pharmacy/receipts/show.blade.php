<div class="space-y-6">
    <div class="flex justify-end">@can('pharmacy.verify-receipt')<x-primary-button wire:click="verify"><x-lucide-check class="h-4 w-4" /> Verify</x-primary-button>@endcan</div>
    <x-card class="grid gap-4 md:grid-cols-4"><div><p class="text-xs text-slate-500">Supplier</p><p class="font-semibold">{{ $receipt->supplier?->name }}</p></div><div><p class="text-xs text-slate-500">Location</p><p class="font-semibold">{{ $receipt->location?->name }}</p></div><div><p class="text-xs text-slate-500">Status</p><p class="font-semibold">{{ $receipt->status?->value ?? $receipt->status }}</p></div><div><p class="text-xs text-slate-500">Received</p><p class="font-semibold">{{ $receipt->received_at?->format('d M Y H:i') }}</p></div></x-card>
    <x-card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-xs uppercase text-slate-500"><th class="py-3">Medicine</th><th>Batch</th><th>Expiry</th><th>Qty</th><th>Cost</th></tr></thead><tbody>@foreach($receipt->items as $item)<tr class="border-t border-slate-100 dark:border-slate-800"><td class="py-3 font-semibold">{{ $item->medicine?->name }}</td><td>{{ $item->batch_number }}</td><td>{{ $item->expiry_date?->format('d M Y') }}</td><td>{{ $item->quantity_received }}</td><td>{{ number_format((float) $item->unit_cost, 2) }}</td></tr>@endforeach</tbody></table></div></x-card>
</div>
